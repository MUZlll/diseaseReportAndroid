<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DiseaseReportAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/gray_deep</item>
        <item name="colorOnPrimary">@color/gray_deep</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/gray_deep</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="ToolBarColor">
        <item name="android:background">@color/black_wx</item>
    </style>

    <style name="bottomNavStyle">
        <item name="android:background">@color/black_wx</item>
    </style>

    <style name="textInputColor">
        <item name="boxBackgroundColor">@color/black_wx</item>
    </style>
</resources>
